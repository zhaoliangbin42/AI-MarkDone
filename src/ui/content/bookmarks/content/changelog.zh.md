# 更新日志

# 4.5.0
2026-06-05

久等了各位。最近我一直想端一桌好菜给大家，所以一直在厨房里忙活。我相信好饭不怕晚，也相信这次确实是一顿好饭。这一版增加了很多实用功能，也解决了不少 bug，还终于补上了一个产品官网。且听我一一道来。

## Google Drive 云备份

首先，最重磅、也是这次开发耗时最长的功能，是 Google Drive 云备份。大家可以在设置里看到它。为了尽量简化流程，这次把 Google Drive 授权做成了更直接的方式：点击之后，按照 Google 的授权页面完成确认即可。

请放心，插件本身不具备任何信息采集功能，也不会采集你的任何信息。你的数据只会保存在本地，或者在你授权之后，保存到你自己的 Google Drive 里。接入云盘之后，你可以随时把书签内容备份到云盘，也可以在另一台设备上点击恢复。

恢复时不会马上写入本地数据，而是会先让你预览恢复之后的合并效果。书签恢复采用的是安全合并方式，会保留你已有的本地书签，只新增云端独有内容。不过，这个功能目前仍然处于实验阶段，所以第一次使用前，还是建议大家先把书签导出一份到本地。

## 发送后位置保持与消息切换

这一版还增加了两个我觉得很实用的功能。

第一，发送消息之后，可以停留在当前阅读位置了，不用每次都被 ChatGPT 强制带到最底部。第二，支持用左右方向键切换上一条 / 下一条消息；如果你不喜欢这个行为，也可以在设置里关闭。

这两个功能解决的都是使用过程中的小痛点：让阅读更专注，让消息切换更快速、更精准。

## 平台与导航收口

还有一个比较重要的变化：当前版本下架了 DeepSeek、Claude 和 Gemini 的页面适配。已经保存的这些平台书签仍然会保留在书签面板里，数据不会丢失。只是受限于精力，后续我会集中维护 ChatGPT。我希望把 ChatGPT 这条链路做到尽善尽美，给大家带来真正高效、稳定、实用的功能。

另外，AI-MarkDone 自己的右侧导航栏也暂时去掉了。它最早是官方没有提供好用导航时的替代品；现在 ChatGPT 官方已经有了自己的导航能力，所以我觉得我们自己的导航栏暂时没有继续存在的必要。后续会继续看大家的反馈。

## 官网

还有一个好消息：拖了很久的插件官网终于也做出来了。

官网地址：https://zhaoliangbin42.github.io/ai-markdone/en/

现在内容还不是特别完善，但我希望能让这个插件更好地传播出去。我相信遇到同样需求的朋友应该还有很多，只是目前还没有一种更好的方式触达他们。也希望大家能帮忙多多传播。插件会一直坚持免费，如果你喜欢，也欢迎去应用商店打分，或者去 GitHub 上点点星星。

## 原理解析

又到了我最喜欢的原理解析环节。我很喜欢把开发过程中发现的问题、学到的知识和总结出来的经验分享给大家，所以这个栏目后续会尽量稳定保留下来。

这次想聊的是一个很烦人的问题：ChatGPT 网页开太久之后，插件有时候会像“失联”了一样，点按钮没反应，或者后台出现找不到 tab 的报错。

这个问题和 Chrome 扩展的运行机制有关。现在 Chrome 扩展大多运行在 MV3 机制下，后台不是一个一直常驻的页面，而是一个事件驱动的 service worker。也就是说，浏览器觉得它暂时没事做的时候，就可能把它停掉；等下一次有事件发生，再把它唤醒。

同时，如果一个 ChatGPT 标签页长时间放在后台，浏览器也可能对它进行冻结、丢弃或延迟恢复。这个时候，后台想给某个 tab 发消息，可能会发现 tab 已经关了、id 过期了，或者页面里的 content script 暂时还没恢复，于是就会出现类似“找不到这个 tab”或者“接收端不存在”的错误。

这次的思路不是强行让后台一直活着。那样虽然看起来简单，但会更耗资源，也不符合浏览器现在的设计方向。我们做的是更轻量的恢复链路：插件在和页面通信前，会先用 ping 确认当前页面是否还连得上；如果连不上，就不硬发命令，也不把这种生命周期里的正常失败当成致命错误。等 ChatGPT 页面恢复之后，content script 会主动发一个 ready 信号给后台，告诉后台“我回来了”。后台再根据这个 tab 的真实 id 更新状态。

简单说，就是不常驻、不轮询、不强行保活，而是在浏览器把页面和后台唤醒之后，让它们重新握手。这个思路我还会继续观察和验证，但我觉得方向是对的：既要恢复得回来，也不要为了恢复而偷偷消耗系统资源。

## 新增

- 重磅增加 Google Drive 备份与恢复功能（实验性）。第一次做，希望大家多多反馈、多多包涵；首次使用前，建议先导出一份本地备份（感谢 GitHub 用户 @eagleshang5-lang 和小红书用户 @Lunas）。
- 增加发送消息后停留在原阅读位置的功能，告别每次发消息都会默认切到底部的问题（感谢小红书用户 @不歸）。
- 右下角换页器支持左右方向键切换消息；按钮和键盘导航都可以在设置中自行关闭。
- 上线官网，欢迎大家帮忙多多传播、多多支持。

## 优化

- 在工具栏中点击导出时，默认不再全部选中，而是只选中当前消息（感谢小红书用户 @momo 和 Email @Johan Song）。
- 右下角换页器改成横向放置，更贴近上一条 / 下一条消息的使用习惯。
- 页面宽度变化时，插件会更快进入临时挂起状态，减少拖动窗口时的重复渲染。

## 调整

- 下架 DeepSeek、Claude 和 Gemini 的页面适配，但已经保存的书签仍会保留在书签面板中，数据不会丢失。受限于精力，后续会集中维护 ChatGPT。
- 暂时去掉 AI-MarkDone 自己的右侧导航栏，保留更轻量的右下角上一条 / 下一条消息切换器。

## 修复

- 解决 ChatGPT 网页开太久之后插件可能失联的问题。

# 4.4.6
2026-05-28

这一版继续推进 Google Drive 备份体验：功能会明确标注为实验性，云端恢复预览会使用和本地导入一致的合并详情页，设置面板里可以把不需要的云端备份移到 Google Drive 回收站，也会显示当前连接账号，但不再把设置面板做成复杂控制台。

## 修复
- 修复导航栏显示卡顿的问题。
- 修复工具栏中的复制按钮与阅读器中的复制按钮链路分岔的问题。具体表现是，在上一个版本中，工具栏的复制按钮有时候复制会失效（感谢小红书用户@momo）

# 4.4.5
2026-05-24

这一版主要围绕公式复制、阅读器长文阅读、工具栏稳定性和 ChatGPT 页面性能做了一轮更新。

## 新增
- 新增点击之后将公式复制为 Office 所支持的 MathML 格式。
- 支持表格内部渲染公式（感谢小红书用户 @小红薯67542）。
- 增加了全局状态指示小弹窗，比如“复制成功”等。
- 在阅读器中增加 Sticky 模式，在正文中选中内容可以在左侧贴住，阅读长文再也不用来回翻找。
- 阅读器现在支持上下方向键滚动消息（感谢小红书用户 @如果你也对吃感兴趣）。

## 变更
- 升级了阅读器里面新页面的刷新逻辑，现在在阅读器中发送消息的时候，新增的页面终于能出来了。
- 升级了工具栏的添加逻辑，当前工具栏的注入应该会更加稳（玄学）。
- 升级了当前目录条的刷新逻辑，大幅提升了性能。
- 升级了当前页面宽度变更时候的工具栏和目录条的渲染逻辑。
- 由于部分用户反馈，鼠标悬浮在公式上方的时候，弹出来的按钮会遮挡阅读，因此，这一版将弹出来的按钮默认关闭，并将按钮的形态做了精简。如有需要的话，直接在设置页中进行开启即可（感谢小红书用户 @如果你也对吃感兴趣）。

## 修复
- 修复了 tooltip 的显示问题，比如复制公式时提示会被覆盖。
- 修复复制为 Markdown 的时候，可能会将代码框中的链接当做噪声清洗掉的问题（感谢 Email @童硕）。

## 原理解析

这次性能优化的重点不是把页面“刷得更快”，而是尽量减少不必要的刷新。ChatGPT 页面本身会不断增量加载、替换节点、补齐工具栏和调整布局，如果插件把每一次 DOM 变化都当成“整页重新分析”的信号，就很容易在长对话里产生额外卡顿。

这一版中，目录条的刷新逻辑会更谨慎地判断变化是否真的影响对话结构；如果只是局部按钮、工具栏或无关节点发生变化，就尽量不触发完整重建。这样在打开长对话、等待消息继续生成，或者 ChatGPT 自己补齐页面元素的时候，目录条不需要反复做同一件事。

工具栏和页面宽度变化也做了类似处理。拖动窗口宽度时，页面会连续触发很多布局变化；以前这些变化可能会和工具栏、目录条的渲染互相干扰。现在会先让页面宽度变化稳定下来，再恢复相关 UI 的刷新，减少拖动窗口时的抖动和重复计算。

阅读器的新页面刷新逻辑也一起升级了。现在在阅读器中继续发送消息后，新增内容会更稳定地进入阅读器页面，不需要反复关闭再打开。整体目标是让插件在 ChatGPT 这种动态页面里少打扰、多等待、只在真正需要的时候更新。

# 4.4.1
2026-05-15

这一版主要围绕“更好看、更可调、更适合长内容阅读”来做。

## 新增
- 在“设置 -> 高级设置”中新增全局字体大小调节，范围固定为 12px-20px，只能通过加减按钮调整，修改后会实时刷新插件界面。
- 在“设置 -> 高级设置”中新增主题色切换，按钮、链接、聚焦态、选中态和不支持页面弹窗都会统一使用选择后的主题色。
- 新增阅读器内部导航栏。阅读长消息时，可以直接在当前消息内部按标题跳转，不需要在正文里反复滚动（感谢小红书用户 @Hiahiaaa 和 @小红薯67542EF1）。
- 新增阅读器内部导航栏开关。如果你更喜欢干净的阅读界面，可以在设置中关闭它。
- 导航栏中，对已经加入书签的消息，会有不同的颜色以作区分，方便快速定位书签消息。

## 变更
- 大幅精简样式系统，阅读器、设置、书签、工具栏、弹窗和提示层现在使用更统一的 token 化视觉规范。

## 修复
- 阅读器最后一条 ChatGPT 消息现在会在打开或重新切回时从实时对话快照刷新。模型还在输出时，不需要关闭阅读器再打开，也能看到更新后的最后一条内容。

# 4.4.0
2026-05-12

感谢 GitHub 用户 @LTong-g 贡献代码，这一版本针对“将消息导出为 PNG”功能做了大幅度优化。

## 原理分享

之前导出为 PNG 的基本思路，是将内容渲染之后按照高度分块。但是实际上，对于包含大量公式、代码块、表格或图片的内容块来说，即使它的视觉高度不一定特别夸张，背后的 DOM 结构也可能非常复杂，单块渲染压力依旧巨大。@LTong-g 的做法是：除了按高度分块外，也会按 DOM 复杂度分块，从而减少复杂消息导致的长时间浏览器卡顿。

这一版也给 PNG 导出补上了进度反馈。复制为 PNG 时会显示可取消的进度面板；批量导出 PNG 时会同时显示当前消息进度和总进度。导出需要等待时，界面应该告诉你它正在做什么，而不是让按钮看起来像卡住了。

欢迎大家反馈或者贡献代码。我希望每次更新都能将原理描述一下，满足大家对原理的好奇心，也激发大家参与进来的热情。

## 变更
- 提升导出 PNG 的性能，并添加进度条，缓解等待焦虑（感谢 GitHub 用户 @LTong-g 贡献代码）。
- 更新内容发现链路。ChatGPT 最近网页元素有所变动，旧的内容发现链路只能探索到最近几条消息，导致阅读器和导出功能都受限，现在问题已修复（感谢小红书用户 @Jim 和 @全是恶意）。

# 4.3.1
2026-05-07

这一版主要修复 Firefox 下的工具栏稳定性问题，并继续降低 ChatGPT 引用内容进入阅读器复制和导出结果的噪声。

## 修复
- 修复 Firefox 中 ChatGPT 工具栏插入失败的问题（感谢小红书用户 @圆桶烤肉拌厚切三文鱼）。
- 进一步对阅读器复制和导出内容中的超链接、文件引用降噪（感谢 GitHub 用户 @LTong-g）。

# 4.3.0
2026-05-05

这一版重点增强公式图片导出、灵动注释复制和 ChatGPT 目录辨识能力，同时继续修复用户反馈的导出与工具栏稳定性问题。

## 新增
- 新增单个公式导出为图片能力，支持 PNG 和 SVG（矢量图），再也不怕做组会 PPT。
- 公式导出为图片的按钮支持在“工具栏与页面操作”设置中自定义开关。
- 灵动注释的“用户 prompt”位置可以自由选择放在注释前面还是后面（感谢小红书用户 @momo）。
- 导航栏展开模式现在支持显示 Prompt 前 15 个字符以及后 15 个字符，可以在设置中选择开启。这样当很多 Prompt 开头相同时，也能更容易区分消息。
- 设置面板将工具栏、公式和导出相关选项归在一起，并补充 v4.5.0 起 Gemini、Claude、DeepSeek 适配下线的提示。

## 修复
- 修复导出为 PDF 时，带编号公式可能会出现文字重叠的问题（感谢 GitHub 用户 @LTong-g）。
- 提升消息底部工具栏的注入稳定性（感谢小红书用户 @去码头整点橙汁）。

# 4.2.3
2026-04-29

这一版主要补充阅读器布局调节和 ChatGPT 消息快捷跳转，同时修复两个用户反馈的细节问题。

## 新增
- 增加阅读器面板宽度管理功能，作为非常用选项放在 Settings 底部（感谢小红书用户 @Z..（研一版））。
- 在 ChatGPT 页面右下角增加上一条/下一条消息快捷跳转功能（感谢小红书用户 @特立独行的猫之工程师）。

## 修复
- 修复阅读器顶部“复制注释”按钮重复显示的问题（感谢 GitHub 用户 @LTong-g）。
- 修复 PDF 导出时带编号公式渲染重叠的问题（感谢 GitHub 用户 @LTong-g）。

# 4.2.2
2026-04-27

这一版继续修复 ChatGPT 内容发现和右侧目录的稳定性问题，同时清理阅读器复制时的引用噪声。

## 修复
- 将阅读器面板中的链接引用从复制内容中删除，减少 Markdown 噪声（感谢 GitHub 用户 @LTong-g）。
- 进一步提升右侧导航条的稳定性，目录预览、点击跳转和滚动定位现在共用同一套用户轮次发现结果（感谢 GitHub 用户 @LTong-g）。
- 修复 Deep Research 内容发现链路遗漏内容的问题。现在右侧导航条在 Deep Research 中也能正确发现轮次并跳转。阅读器对于 Deep Research 的内容会显示为空，因为官方的 Deep Research 阅读体验已经很好，这里暂时不单独适配；如果大家有需求，欢迎随时反馈。
- 修复 v4.2.1 中阅读器获取 ChatGPT 内容失败的问题（感谢小红书用户 @小红薯67542EF1）。

# 4.2.1
2026-04-27

这一版修复 ChatGPT 更新后右侧目录定位不稳定的问题。

## 修复
- 修复右侧目录有时能显示正确列表，但点击跳转或滚动高亮无法稳定对应到正确对话位置的问题。

## 原理解析
- ChatGPT 更新后，旧的对话容器不再稳定出现。
- 之前目录显示和页面定位各用一套判断，所以会出现错位。
- 现在先统一生成“用户轮次 + 页面锚点”的位置模型。

# 4.2.0
2026-04-26

看了网友的反馈，我发现 Gemini、DeepSeek 和 Claude 平台其实都已经存在一些适配问题。结合我现有的精力想了想，与其做大而全，不如好好把精力聚焦在 ChatGPT 这一个平台上。实际上，ChatGPT 上已经有足够多的效率痛点需要解决，并且这也是我平时使用的主力平台，因此我的体会也是最深的。

实践出真知。我认为只有当我深入体会之后，才能更有针对性地做出符合真实场景的提效功能。并且，现有市场上也有一些项目兼顾了这些需求。再三考虑后，我决定从 v4.5.0 开始下线 Gemini、DeepSeek 和 Claude 适配，专注于 ChatGPT，让 ChatGPT 成为 AI-MarkDone 唯一支持的平台。

当然，这几个平台也有替代插件，欢迎大家下载使用。

对于 Gemini，目前做得最好的插件应该是 [Gemini Voyager](https://github.com/Nagi-ovo/gemini-voyager)。

对于全平台书签功能，我之前用过一个非常喜欢的插件是 [Timeline](https://github.com/houyanchao/Timeline)。

我相信这两个插件在另外几个平台中应该能满足大家绝大多数的需求。当然，我也会在 ChatGPT 中沿着最初的宗旨继续前进，在保持克制、不打扰的前提下，开发出一个从理工科研究生角度来说真正趁手的 ChatGPT 插件，帮助大家提高使用效率，补齐 ChatGPT 体验上最后一公里的短板。

这次更新还增加了两个非常实用的功能，分别是导出消息为图片，以及导航栏的展开模式。其中，导出消息为图片是我个人比较喜欢的功能。平时，ChatGPT 消息分享主要还是以链接分享或者截图分享为主，但是对于很多内容比较长的对话，就需要用到长截图，上手难度会大幅增加。因此，在充分考虑用户分享场景后，我给插件添加了一键复制消息为图片以及批量导出消息为图片这两个功能。

另外，之前导航栏调的手风琴效果，好看是好看，但实用性欠佳。因为如果窗口中有几十条消息，我们无法一眼定位到想要的那条消息，而是要来回滑动去寻找。因此这个版本中引入了导航栏的全展开模式，方便用户一眼定位消息。

## 新增
- 新增对 Safari 浏览器和 Firefox 浏览器的支持。
- 增加消息批量渲染成图片功能（感谢小红书用户 @小锋iDyll）。
- 新增消息直接复制成图片功能，适合快速将消息分享给朋友（感谢小红书用户 @小锋iDyll）。
- 消息渲染成图片的宽度、清晰度可以在设置中进行修改，以适应目标分享平台的最佳视觉宽度。
- 新增导航栏的展开模式，方便一次性看清所有消息的内容摘要。
- 新增导航栏的开关，方便用户关闭导航栏。

## 修复
- 修复设置面板中的布局问题。
- 修复书签文件命名的 invalid 提示不够明确的问题（感谢小红书用户 @Colin的AI杠杆）。
- 修复导航栏点击一次无法正确定位到目标消息的问题（感谢小红书用户 @谦卑于世）。

## 原理解析
- 之前导航栏点击一次之后，会跳转到对应消息所在的位置。但是这个时候会触发 ChatGPT 官方的增量加载，而加载出来的内容会导致布局有所偏移，进而导致定位也发生偏差。所以之前导航栏需要点击第一次并跳转到对应位置之后，等网页稳定下来，再点击一次才能到达目标位置。现在的方案是点击一次之后，会先尝试跳转到对应的位置，然后监测增量加载之后的坐标变化，再进行一次跳转。第二次跳转对用户来说几乎无感。

# 4.1.2
2026-04-22

2026年4月22日凌晨，ChatGPT进行了一次较大的版本更新，导致页面内信息的全量加载变成了增量加载，原有的信息识别算法全部失效，影响范围包括但不限于：折叠条功能直接失效、阅读器只能识别到最终渲染的4~5条消息等。

鉴于最初开发折叠条的目的是为了提高页面的加载速度，降低渲染压力，如今 ChatGPT 已经官方支持增量加载，会自动卸载非活跃节点，性能得到大幅提升。另外也由于 ChatGPT 的动态加载方式，导致折叠条的开发复杂度大幅提高，稳定性大幅降低。经过综合评估，决定将折叠条功能删除。

关于阅读器，由于当前网页默认只加载视窗内的4~5条消息，以往获取页面内稳定节点的方法已经失效，阅读器无法一次性将所有内容全部展示。为了解决这个问题，针对 ChatGPT 重新设计了内容发现引擎，现在已经具备进入页面之后，阅读器能识别所有消息的能力。

对于官方的浏览页面，经过我的实际使用，我感觉这次更新后的动态加载也有一些不太顺手的地方。比如滚动条快速拉动时，内容显示会有延迟，导致旧消息翻看难度提升。因此，为了照顾喜欢使用原生页面的用户，我顺带将当前的底层能力提取出来做成了目录。进入对话页后，目录将会在右侧展示，支持实时消息预览，并且点击之后能够直接跳转到想要的位置。

## 新增
- 阅读器中的“灵动注释”现在支持“删除注释”（感谢小红书用户 @Z..（研一版））
- 新增 ChatGPT 内右侧目录功能，可以一键跳转到对应对话。

## 变更
- 针对 ChatGPT 新的增量加载机制，重新设计了阅读器的内容发现引擎。
- 调整了 ChatGPT 场景下的书签保存、高亮和跳转策略，使书签与右侧目录共用同一套稳定的对话位置模型。

## 修复
- 修复书签名称无法重命名的问题（感谢小红书用户 @Z..（研一版））
- 修复 ChatGPT 增量加载下书签保存、高亮和跳转定位错误的问题
- 修复 ChatGPT 内阅读器内容获取失败的问题（感谢小红书用户 @沐霖）
- 修复阅读器中发送框内，插入 prompt 功能失效的问题
- 改进书签跳转定位，使 ChatGPT 书签与右侧目录共用同一套稳定锚点链路（感谢小红书用户 @Z..（研一版））。

## 移除
- 删除 ChatGPT 消息折叠功能

# 4.1.1
2026-04-20

这一版主要修复阅读器发送弹窗里的“插入注释”问题。现在选择提示词时，发送弹窗不会再被误判为外部点击而提前关闭，整理好的注释内容可以正常插入到发送框中。

## 修复
- 修复阅读器发送弹窗里的“插入注释”问题。现在选择提示词时，发送弹窗不会再被误判为外部点击而提前关闭。

# 4.1.0
2026-04-19

这一版的重点有三件事。第一，独立的源码复制面板被删掉了，源码复制能力直接并进了阅读器。第二，新增了“灵动注释”，可以边读边标、边写修改意见，再一次整理成适合发给大模型的输入。第三，书签面板补上了 Changelog、About 和 FAQ 三个信息页，方便查看版本变化、项目背景和常见问题。

其中，把源码复制直接搬到阅读器里，是这次最难做的一部分。公式、代码块、表格、图片这类内容，并不是普通文本，它们更像一个个需要整体处理的闭合单元。如果直接沿用浏览器默认复制，最后拿到的往往只是渲染后的可见文本，原来的 Markdown 结构很容易丢掉。现在的做法，是在阅读器里先识别这些闭合单元对应的源码边界；当你选中它们时，复制逻辑会优先回到原始 Markdown 片段，再按选区顺序重新拼起来，所以复制结果会尽量保持标准 Markdown，而不只是页面上看见的那层文本。

## 新增
- 阅读器里的源码复制现在直接可用，不再需要单独打开源码面板。
- 阅读器支持对代码、公式、表格、图片等闭合内容按源码进行选择和复制，复制结果会尽量保持标准 Markdown 形式。
- 阅读器现在支持对选中内容直接添加灵动注释。选中正文后，会在附近出现悬浮的复制和注释按钮。
- 注释会和对应选区建立关联，并在阅读器中以高亮方式持续标记出来；已有注释也支持再次打开查看和编辑。
- 新增注释导出模板，可在“设置 > 阅读器 > 注释复制模板”中自定义每条注释的导出格式。
- 新增 Prompt Library，可在“设置 > 阅读器 > 用户提示词”中维护多条可复用的自定义提示词。
- 注释结果支持一键复制，也可以直接插入阅读器左下角的发送消息框上方，减少手动复制粘贴。
- 书签面板新增 Changelog、About 和 FAQ 信息页，帮助你更快了解版本变化和项目背景。

## 变更
- 将源码复制能力直接并进阅读器，不再保留独立源码面板作为主要入口。
- 优化 unsupported 页面下的插件 popup，去掉冗余内容，整体样式也更清晰。

## 移除
- 删除独立的源码复制面板。

# 4.0.0
2026-04-05

## 变更
- 完成了一轮底层架构重构，整体行为更稳定，后续扩展也更清晰。
- 统一了主要面板、弹窗、按钮和提示的视觉风格，界面更协调，也更接近同一套产品语言。
- 页头书签入口和工具栏交互进一步统一，在不同平台上的位置和行为更稳定。
- Markdown 渲染链路做了统一，代码、公式、表格等内容在不同界面中的表现更加一致。

## 修复
- 修复了 ChatGPT 消息获取被截断的问题，长内容的提取更完整。
- 修复了 Claude 和 Gemini 上工具栏注入不稳定的问题。
- 修复了发送弹窗与原生输入框之间的同步问题，输入体验更可靠。
- 修复了若干阅读器、书签和弹窗相关问题，整体使用体验更顺手。

# 3.0.0
2026-02-19

## 新增
- 支持 ChatGPT、Gemini、Claude 和 DeepSeek 等主流 AI 对话平台。
- 提供专门的阅读器模式，把长对话整理成更适合连续阅读的页面。
- 支持将对话内容复制为结构更完整的 Markdown，方便整理、保存和二次使用。
- 提供字数统计能力，帮助快速判断回复长度和信息密度。
- 内置书签系统，可以保存、分类和回看重要对话。
- 提供设置面板，用来控制阅读器、书签和工具栏等主要功能。
- 支持导出 Markdown 和 PDF，方便归档与分享。
- 为 ChatGPT 提供消息折叠能力，支持折叠旧消息以及一键全部折叠或展开。
