{
    "name": "ai-markdone",
    "private": true,
    "version": "2.9.0",
    "description": "为 ChatGPT 和 Gemini 提供沉浸式阅读模式、书签管理、LaTeX 公式一键复制、Markdown 导出等功能，提升大模型使用体验。",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "npm run build:chrome && npm run build:firefox",
        "build:chrome": "tsc && vite build --config vite.config.chrome.ts && npm run postbuild:chrome",
        "build:firefox": "tsc && vite build --config vite.config.firefox.ts && npm run postbuild:firefox",
        "postbuild:chrome": "cp manifest.chrome.json dist-chrome/manifest.json && mkdir -p dist-chrome/icons && cp public/icons/*.png dist-chrome/icons/ && mkdir -p dist-chrome/src/popup && cp src/popup/popup.html dist-chrome/src/popup/",
        "postbuild:firefox": "cp manifest.firefox.json dist-firefox/manifest.json && mkdir -p dist-firefox/icons && cp public/icons/*.png dist-firefox/icons/ && mkdir -p dist-firefox/src/popup && cp src/popup/popup.html dist-firefox/src/popup/ && cp src/background/background-firefox.js dist-firefox/background.js",
        "preview": "vite preview",
        "type-check": "tsc --noEmit",
        "test": "vitest",
        "test:ui": "vitest --ui"
    },
    "dependencies": {
        "marked": "^17.0.1",
        "marked-katex-extension": "^5.1.6",
        "webextension-polyfill": "^0.12.0"
    },
    "devDependencies": {
        "@crxjs/vite-plugin": "^2.0.0-beta.21",
        "@playwright/test": "^1.58.0",
        "@testing-library/dom": "^10.4.1",
        "@testing-library/user-event": "^14.6.1",
        "@types/chrome": "^0.0.258",
        "@types/dompurify": "^3.2.0",
        "@types/jsdom": "^21.1.7",
        "@types/node": "^24.10.2",
        "@types/webextension-polyfill": "^0.12.4",
        "@vitest/ui": "^4.0.16",
        "autoprefixer": "^10.4.16",
        "dompurify": "^3.3.1",
        "jsdom": "^23.2.0",
        "postcss": "^8.4.32",
        "sinon": "^21.0.1",
        "tsx": "^4.21.0",
        "typescript": "^5.3.3",
        "vite": "^5.0.10",
        "vite-plugin-static-copy": "^3.1.4",
        "vitest": "^4.0.16"
    }
}